for i in range(1, 10):
    for j in range (1,i+1): #Looks like an echelon
        print ("%d*%d=%d" % (j, i, j*i), end="\t" )   # end = \t means Add a Table mark
    print() #move the data in the format
